Senior interviews (especially in Rust) expect:

- Clean API design under time pressure
- Ownership-aware modeling
- Trait-based extensibility
- Error modeling
- Concurrency correctness
- Tradeoff articulation
- Clean testing strategy
- Incremental refactoring

========================================================
🔥 How You Should Practice
========================================================

For each 30-minute session:

1. Spend 5 minutes designing the API on paper
2. Code cleanly
3. Add 2–3 unit tests
4. Spend 5 minutes explaining tradeoffs aloud
5. Identify how you'd productionize it

This mimics senior interviews far better than pure algorithm grind.

========================================================
🦀 Senior Rust 30-Minute Interview Challenges
========================================================

Each is scoped to fit ~30 minutes but forces architectural decisions.


1. In-Memory Key-Value Store
--------------------------------------------------------
Implement:

pub struct KvStore { ... }

impl KvStore {
    pub fn new() -> Self
    pub fn set(&mut self, key: String, value: String)
    pub fn get(&self, key: &str) -> Option<&String>
    pub fn delete(&mut self, key: &str) -> bool
}

Senior signal:
- API clarity
- Borrowing decisions
- &str vs String ergonomics
- Testability

Extension: Add TTL support.


2. Thread-Safe Counter Service
--------------------------------------------------------
Implement a counter shared across threads:

pub struct Counter { ... }

impl Counter {
    pub fn inc(&self)
    pub fn get(&self) -> usize
}

Spawn 10 threads incrementing it.

Senior signal:
- Arc
- Mutex vs AtomicUsize tradeoffs
- Contention discussion


3. Mini LRU Cache (Real Version)
--------------------------------------------------------
Capacity-bound LRU:

- get
- put
- Evict least recently used

Senior signal:
- HashMap + VecDeque or LinkedHashMap pattern
- Complexity discussion
- API design


4. Generic Rate Limiter (Token Bucket)
--------------------------------------------------------
pub struct RateLimiter { ... }

pub fn allow(&mut self) -> bool

Senior signal:
- Time handling
- Instant vs SystemTime
- Edge cases
- Deterministic testing discussion


5. Generic Binary Search Tree
--------------------------------------------------------
- Insert
- Contains
- Inorder traversal iterator

Senior signal:
- Ownership modeling
- Option<Box<Node<T>>>
- Generics with Ord


6. Simple Logger
--------------------------------------------------------
- Log levels enum
- Thread-safe
- Output to stdout

Senior signal:
- Enum design
- Trait implementations
- API ergonomics


7. Mini Thread Pool
--------------------------------------------------------
pub struct ThreadPool { ... }

pub fn execute<F>(&self, job: F)
where
    F: FnOnce() + Send + 'static

Senior signal:
- mpsc channels
- Worker lifecycle
- Drop handling


8. TryFrom<&str> for Domain Type
--------------------------------------------------------
Example:

struct Email(String);

Validate format.

Senior signal:
- Custom error enum
- From / TryFrom traits
- Encapsulation of invariants


9. Streaming Median
--------------------------------------------------------
Insert numbers one-by-one and return current median.

Senior signal:
- Two-heap solution
- Complexity analysis
- Ownership modeling


10. File-Based Config Loader
--------------------------------------------------------
- Load JSON or TOML
- Parse into struct
- Graceful error handling

Senior signal:
- Result propagation
- Layered error modeling
- Separation of parsing vs IO


11. Custom Iterator Implementation
--------------------------------------------------------
Example:

pub struct EvenNumbers { ... }

Implement Iterator.

Senior signal:
- State ownership
- Iterator trait mastery
- Lifetimes if borrowing


12. Simple Router
--------------------------------------------------------
Given routes like:

GET /users/:id
POST /users

Match request path to handler.

Senior signal:
- Pattern matching
- Data modeling
- Clean separation of routing vs handling


13. Expression Evaluator
--------------------------------------------------------
Evaluate:

"3 + 4 * 2"

Senior signal:
- Stack modeling
- Tokenization vs evaluation separation
- Error handling


14. Graph with BFS and DFS
--------------------------------------------------------
- Adjacency list
- Implement both traversals

Senior signal:
- Borrowing with graph structures
- Visited tracking
- Complexity reasoning


15. Plugin System with Traits
--------------------------------------------------------
trait Plugin {
    fn name(&self) -> &str;
    fn execute(&self, input: &str) -> String;
}

Store heterogeneous plugins in a vector.

Senior signal:
- Trait objects (dyn)
- Extensibility tradeoffs
- Object safety awareness


16. Debouncer
--------------------------------------------------------
Given events happening rapidly, only process last event after delay.

Senior signal:
- Threading
- Timing
- Channel coordination


17. Mini Metrics Collector
--------------------------------------------------------
- Counter
- Gauge
- Histogram (simplified)

Expose:

record_counter(name, value)

Senior signal:
- Data modeling
- Concurrency
- API design


18. Safe Wrapper Around Unsafe API
--------------------------------------------------------
Mock unsafe pointer usage and encapsulate safely.

Senior signal:
- Safety boundaries
- Documentation
- Justifying unsafe blocks


19. Generic Retry Mechanism
--------------------------------------------------------
fn retry<F, T, E>(f: F, attempts: usize) -> Result<T, E>

Senior signal:
- Generics
- FnMut vs FnOnce
- Backoff strategy discussion


20. Bounded Queue
--------------------------------------------------------
- Fixed capacity
- Blocking or non-blocking version

Senior signal:
- Mutex + Condvar
- Deadlock awareness
- Backpressure discussion


21. Mini Scheduler
--------------------------------------------------------
Schedule tasks to run at certain times.

Senior signal:
- Time handling
- Priority queue usage
- Concurrency coordination


22. Domain Model with Invariants
--------------------------------------------------------
Example:

struct BankAccount { ... }

Enforce:
- No negative balance
- Immutable account ID

Senior signal:
- Encapsulation
- Preventing invalid states
- API safety


23. HTTP Client Abstraction
--------------------------------------------------------
trait HttpClient {
    fn get(&self, url: &str) -> Result<Response, Error>;
}

Provide mock implementation.

Senior signal:
- Dependency inversion
- Testability
- Trait object usage


24. Memory-Efficient Deduplicator
--------------------------------------------------------
Given stream of strings, deduplicate with bounded memory.

Senior signal:
- Hashing tradeoffs
- Bloom filter discussion
- Space/time tradeoffs


25. Lock-Free Stack (Simplified)
--------------------------------------------------------
Using AtomicPtr.

Senior signal:
- Memory ordering discussion
- Safety explanation
- Advanced concurrency reasoning


26. Circuit Breaker
--------------------------------------------------------
States:
- Closed
- Open
- HalfOpen

Senior signal:
- State machine modeling
- Time-based transitions
- Production reliability discussion


27. Simple Event Bus
--------------------------------------------------------
- Subscribe
- Publish
- Multiple listeners

Senior signal:
- Trait bounds
- Thread safety
- Arc usage


28. Generic Cache with Expiration
--------------------------------------------------------
Keyed by K, V with TTL.

Senior signal:
- Generics
- Trait bounds
- Background cleanup thread discussion


29. Small Domain-Driven Module
--------------------------------------------------------
Example: Order system.

- Order
- OrderItem
- Status transitions

Enforce valid transitions.

Senior signal:
- Enum state transitions
- Invariant modeling
- Clean module boundaries


30. Refactor a Bad API
--------------------------------------------------------
Start with:

fn process(data: Vec<String>) -> Vec<String>

Refactor into:
- Smaller traits
- Error types
- Better naming
- Borrowing improvements

Senior signal:
- Refactoring ability
- API thinking
- Code quality under time constraint

31. Borrowing-Based Text Index (Lifetimes Focus)
--------------------------------------------------------
Given a large input string, build an index of words to positions
without allocating new Strings.

pub struct WordIndex<'a> {
    text: &'a str,
    index: HashMap<&'a str, Vec<usize>>,
}

impl<'a> WordIndex<'a> {
    pub fn new(text: &'a str) -> Self
    pub fn positions(&self, word: &str) -> Option<&[usize]>
}

Senior signal:
- Explicit lifetime annotations
- &str slicing without allocation
- Borrow checker reasoning
- Zero-copy design tradeoffs



32. Async Job Runner (Tokio)
--------------------------------------------------------
Execute async jobs with bounded concurrency.

pub struct JobRunner { ... }

impl JobRunner {
    pub fn new(limit: usize) -> Self
    pub async fn run<F>(&self, job: F)
    where
        F: Future<Output = Result<(), Error>> + Send + 'static;
}

Senior signal:
- Semaphore for concurrency control
- Send across await points
- Spawn vs inline await tradeoffs
- Cancellation discussion



33. Interior Mutability Cache
--------------------------------------------------------
Cache computed value lazily using interior mutability.

pub struct LazyCache<T> { ... }

impl<T> LazyCache<T> {
    pub fn new() -> Self
    pub fn get_or_init<F>(&self, f: F) -> &T
    where
        F: FnOnce() -> T;
}

Senior signal:
- RefCell vs Mutex vs OnceCell tradeoffs
- Borrow rules at runtime
- Thread-safe vs non-thread-safe versions



34. Graceful Shutdown Worker System
--------------------------------------------------------
Spawn worker tasks that process messages and shut down cleanly.

pub struct WorkerPool { ... }

impl WorkerPool {
    pub fn new(size: usize) -> Self
    pub fn submit(&self, job: Job)
    pub async fn shutdown(self)
}

Senior signal:
- Channel closing semantics
- JoinHandle management
- Drop behavior
- Cancellation coordination



35. Zero-Copy CSV Line Parser
--------------------------------------------------------
Parse a single CSV line into fields without allocating new Strings.

pub fn parse_line(input: &str) -> Result<Vec<&str>, ParseError>

Senior signal:
- String slicing
- Lifetime propagation
- Edge case handling
- Performance discussion



36. Minimal Axum REST Service
--------------------------------------------------------
Implement:

POST /users
GET  /users/:id

In-memory storage.

Requirements:
- JSON input/output
- Proper HTTP status codes
- Shared state via Arc

Senior signal:
- Extractors
- State injection
- Error-to-response mapping
- Transport vs domain separation



37. JWT Authentication Middleware (Axum)
--------------------------------------------------------
- POST /login -> returns JWT
- Protected route requiring valid token
- Middleware or custom extractor for auth

Senior signal:
- Request extensions
- Token validation
- Error mapping to HTTP
- Secret handling discussion
- Expiration & clock skew reasoning



38. Async Rate Limiter Middleware
--------------------------------------------------------
Per-IP rate limiting in Axum.

- Wrap existing handler
- Track request timestamps
- Reject with 429

Senior signal:
- Shared async state
- Mutex vs RwLock tradeoffs
- Time handling with Instant
- Memory growth discussion



39. Repository Trait with Dependency Injection
--------------------------------------------------------
Define:

trait UserRepository {
    fn find(&self, id: UserId) -> Result<User, RepoError>;
}

Inject into service layer via Arc<dyn UserRepository + Send + Sync>.

Senior signal:
- Trait object usage
- Send + Sync bounds
- Testability via mock implementation
- Inversion of control



40. Versioned API Payload Handling
--------------------------------------------------------
Support two versions of incoming JSON payload:

enum CreateUserRequest {
    V1(V1Payload),
    V2(V2Payload),
}

Convert both into a unified domain model.

Senior signal:
- Enum modeling
- Validation layering
- Backward compatibility strategy
- Error mapping philosophy